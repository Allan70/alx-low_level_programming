#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * _realloc - reallocates memory block with malloc and free.
 * @ptr: previously allocated pointer to memory
 * @old_size: byte size of the old memory allocated
 * @new_size: new size in bytes to be allocated
