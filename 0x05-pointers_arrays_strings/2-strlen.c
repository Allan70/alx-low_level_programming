#include "main.h"
#include <stdio.h>

/**
 * _strlen - entry point
 * @s: Takes in a string
 *i Description: Resturs the string length
 * Return: Returns the string length
 */
int _strlen(char *s[])
{
	return (strlen(*s));
}
